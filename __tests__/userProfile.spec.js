/*import { showUser } from "../controllers/usersProfileController.js"

const mockReq = {
    id: "678be7b465bfc8961043293c"
};

const mockRes = {
    status: jest.fn(),
    send: jest.fn(),
}

describe('show user', () => {
    it('should find user by id', () => {
        showUser(mockReq, mockRes)
    })
})*/